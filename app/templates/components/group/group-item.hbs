<div class="card {{activeClass}}" {{action onclick this}}>
  <div class="card-header">
    <p class="card-title"> {{group.name}} </p>
    <span class="card-header-info" data-tooltip="Quantidade de praticantes">
      <i class="icon-people"></i> {{group.users_count}}
    </span>
    <span class="card-header-info" data-tooltip="Último treinamento">
      <i class="icon-event"></i> {{group.last_training_date}}
    </span>
  </div>
  <div class="card-content">
    {{group.description}}

    <div class="card-action">
      {{#link-to 'groups.show' group.id class='btn-primary'}}
        VER TURMA
      {{/link-to}}
    </div>
  </div>
</div>